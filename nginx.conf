server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta desconocida vuelve a index.html (react-router)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
